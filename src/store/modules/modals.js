const state = {
  isLoaderVisible: false,
};
const getters = {};
const mutations = {
  setIsLoaderVisible(state, payload) {
    state.isLoaderVisible = payload;
  },
};
const actions = {};

export default {
  state,
  getters,
  mutations,
  actions,
};
