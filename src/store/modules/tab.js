const state = {
  activeTab: "home",
};
const getters = {};
const mutations = {
  setActiveTab(state, payload) {
    state.activeTab = payload;
  },
};
const actions = {};

export default {
  state,
  getters,
  mutations,
  actions,
};
